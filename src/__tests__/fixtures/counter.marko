class {
  onCreate() {
    this.state = { count: 0 };
  }

  increment() {
    this.state.count++;
  }
}

<div class="counter">
  Value: ${state.count}
  <button on-click('increment')>Increment</button>
</div>